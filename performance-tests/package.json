{
  "name": "parking-management-performance-tests",
  "version": "1.0.0",
  "description": "Testes de carga para o sistema de gerenciamento de estacionamento",
  "scripts": {
    "test": "k6 run",
    "test:smoke": "k6 run --vus 1 --duration 30s",
    "test:load": "k6 run --vus 10 --duration 60s",
    "test:stress": "k6 run --vus 30 --duration 120s",
    "test:spike": "k6 run --vus 50 --duration 30s"
  }
}
