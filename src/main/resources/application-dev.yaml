spring:
  docker:
    compose:
      enabled: true
      file: src/main/docker/docker-compose.yml
      profiles:
        active: local
      start:
        arguments: -d
      stop:
        command: down
  devtools:
    livereload:
      enabled: true
    restart:
      poll-interval: 5s
